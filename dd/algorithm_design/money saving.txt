money saving

problem: modify charging area during travel? I'd say fuck no.

input:  destination of the user
		occupation of charging areas

output: charging area


FUCKING ALGO:
areas = getChargingAreasNearDestination(destination, n);

minPercentage = 100
designedChargingArea = None
for c in areas:
	currentPercentage = c.busyPlugs / c.totalPlugs
	if currentPercentage < minPercentage:
		minPercentage = currentPercentage
		designedChargingArea = c

return designedChargingArea